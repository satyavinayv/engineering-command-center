NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
# Must match API_KEY in backend/.env. Deliberately NOT prefixed with
# NEXT_PUBLIC_ - it's only read server-side (see lib/api.ts) and never
# shipped to the browser.
API_KEY=changeme-choose-a-long-random-string

